#include "main.h"

/**
 * main - Test a function that prints whether a number
 * is positive or negative
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int n;

	n = 0;
	positive_or_negative(n);

	return (0);
}
