#include "main.h"

/**
 * _abs - Print the absolute value of an integer
 * @x: integer passed.
 * Return: Always 0 (Sucess)
 */
int _abs(int x)
{
	int x;

	if (x < 0)
	{
		return (x * -1);
	}
	return (0);
}
